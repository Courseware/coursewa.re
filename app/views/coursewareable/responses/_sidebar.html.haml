.four.columns
  .row.sidebar
    %h5
      = _('Response details')
    %hr

    %p.smaller
      = _('Last time updated by %s on %s') % [@response.user.name, @response.updated_at.to_s(:pretty)]

    - unless @response.stats.blank?
      %p
        Quiz coverage is:
        = ("%.2f%" % @response.coverage)

      %p
        You did
        = @response.stats[:wrong]
        answers wrong from
        = @response.stats[:all]
        possible correct answers.

    %pre
      TODO:
      Relevant assignment
      Relevant lecture
      Who created it
      When created
      Relevant assignment
      Relevant lecture
      Who created it
      When created
