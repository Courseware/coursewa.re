= _('Hello, %{name}') % {:name => @user.name}
================================================

= _('You have requested to reset your password.')

= _('To choose a new password, just follow this link: %{url}.') % {:url => edit_password_path(@user.reset_password_token)}

= _('Thanks for teaching with Coursewa.re')
