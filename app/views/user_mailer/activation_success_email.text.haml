= _('Welcome to Courseware, %{username}') % {:username => @user.username}
================================================

= _('You have successfully activated your account: %{username}') % {:username => @user.username}

= _('To login to the site, just follow this link: %{url}.') % {:url => @url}

= _('Thanks for teaching with Coursewa.re')
